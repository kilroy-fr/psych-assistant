services:
  psych-assistant:
    build: .
    container_name: psych-assistant
    expose:
      - "5000"
    ports:
      - "127.0.0.1:5005:5000"  # Direkter Zugriff für Entwicklung
    environment:
      - OLLAMA_HOST=http://ollama:11434
      - TZ=Europe/Berlin
      # 1 = Inhalte (Pass-1-Vorschau, SORC-Block, gefundene Namen) in data/debug_results.log.
      # Nur fuer Fehlersuche, das Log enthaelt dann Echtdaten.
      - LOG_PATIENT_CONTENT=0
    networks:
      - ollama-net
      - caddy-proxy-network
    volumes:
      - ./data:/app/data/data
      - ./storage:/app/storage
    restart: unless-stopped
    labels:
      - "com.centurylinklabs.watchtower.enable=true"

networks:
  ollama-net:
    external: true
  caddy-proxy-network:
    external: true
    name: caddy-proxy-network
